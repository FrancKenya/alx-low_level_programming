$include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @x: the integer
 * Return: the abs of the integer
 */
int _abs(int x)
{
	if (n >= 0)
	{
		return (x);
	}
	{
	else
		return (-x);
	}
}
